[package]
name = "keva_ffi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[dependencies]
keva_core = { path = "../core" }
keva_search = { path = "../search" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
