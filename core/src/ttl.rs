pub(crate) mod ttl_table {
    mod common {
        //! Generic redb Ttl table definition
    }
    pub mod trash {
        // Trash ttl table definition with trash table name
    }
    pub mod purge {
        // Purge ttl table definition with purge table name
    }
}
